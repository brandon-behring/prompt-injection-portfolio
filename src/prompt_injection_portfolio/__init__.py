"""prompt-injection-portfolio — methodology case study (pre-alpha at v0.1.0-pre).

Per plan §3: src/ holds project-specific glue (lane orchestration, data
loaders composing eval-toolkit primitives, project-named CLI wrappers).
Reusable primitives belong upstream (eval-toolkit / runpod-deploy /
research_toolkit / book-scaffold-astro) per Round 10 library-first
invariant.

This is an empty placeholder at v0.1.0-pre; lane modules populate at M1+.
"""

from __future__ import annotations

__version__ = "0.1.0-pre"
