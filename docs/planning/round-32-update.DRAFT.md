# Round 32 update — DRAFT (not yet folded into PORTFOLIO_PLAN.md)

> **DRAFT — awaiting present-first ratification (2026-06-11).** Insert the section below into
> `docs/planning/PORTFOLIO_PLAN.md` directly after the Round 31 update (after line 728, before the
> `---` at line 730). The user ratifies; this file never edits the plan itself.

### Round 32 update — C1 verdict in: NOT-CLOSED at the `lora` ceiling (targeted table data does not bridge) + 5-verifier audit ROBUST + C2 mechanism pre-reg drafted (2026-06-11)

First round after Round 31 decided Fork C = C1. Folds three input streams: the C1 decision datum (the treated-arm `lora` rung, run today under the pre-registration ratified 2026-06-10), its 5-verifier adversarial audit (`experiments/carrier-table-training/AUDIT_C1_2026-06-11.md`), and the C2 mechanism pre-registration drafted during the GPU wait (as Round 31 scheduled). This round records the datum and registers one spine fork; it does **not** re-sequence milestones (still M0→M7) and files **no new ADR** (the ADR-055 amendment is the registered fork below, not a decision of this round — mirrors Round 27/31's no-ADR pattern).

**Decisions locked** (4):
- **C1 (the Lane-2 carrier/table training arc) decision datum is IN — verdict `NOT-CLOSED`**, judged by the pre-locked rule (fixed at ratification 2026-06-10, pre-datum) at the `lora` ceiling. The treated-arm `lora` rung ran on a RunPod H100 (26.8 min, **$1.47**; arc total **$1.74** incl. the $0.27 OpenAI corpus). Per `c1_verdict.json`: **G_control(lora) +0.2054 vs G_treated +0.2333 → ΔG −0.0279, CI-low −0.0319 ≤ 0 ⇒ NOT-CLOSED** — targeted, leakage-gated, format-matched table data does not bridge the residual table wall at the `lora` ceiling. The **frozen-rung reduction (ΔG +0.0830, CI-low +0.0788 — CI-supported; treated wall persists at +0.251) did not survive the decision rung.** Phrasing bounds (audit F5, binding): there is **no evidence of reduction at `lora`**, and **the negative direction is not seed-robust** (−0.006 without control seed-0; control seed-0 test_roc 0.8366 vs 0.7728/0.7690 — the payload-CI is conditional on the 3 seeds) — never "the treated wall grew" (the W13 lesson). tfidf: G −0.148 → −0.013 is a **val-side move** (val_roc 0.78→0.91; held-table test flat ~0.92–0.93) — the ratified carrier-LODO reading "no wall at tfidf — lexical features carrier-invariant" **stands**. W10 dual reading carried in the verdict artifact: sign-only "wall persists (G=+0.233)"; the ½·G(frozen) knob holds (0.233 ≥ 0.126; knob-choice-robust). The pre-registered B+ analogy (the criteria's NOT-CLOSED branch) holds **at the pattern level only** — say "does-not-bridge **at the `lora` ceiling**", not "same mechanism": the axes differ (B+ = capacity-resistant cross-family; C1 = capacity-attenuated carrier), and C1's intervention *did* reduce the frozen rung — a ceiling-specific failure B+ never showed. Pre-registered NotInject secondary: **no over-defense price paid — and no wall bought down** (treated `lora` notinject mean 0.705 vs control 0.729, n=113; clean-context FPRs seed-unstable in both arms — carry both caveats). Record: `experiments/carrier-table-training/{criteria.md, c1_verdict.json}`.
- **5-verifier adversarial audit (the B4 pattern) → ROBUST.** Role verdicts: **REPRODUCES** exact (all 9 point values Δ=0.0 at 6 decimals; independent paired bootstrap concurs, CI-low deviation ≤ 0.0001) / **SOUND** (sha chain end-to-end; test parquets row-identical across arms) / **LEAK-FREE** (0 exact / 0 normalized / 0 near-dup for all 2400 corpus rows vs val+test, **all seeds 0/1/2** — full re-derivation beyond the committed seed-0 gate) / **COMPLIANT** (0 material deviations across 10 commitments; criteria append-only, Revision 1 pre-datum) / **CORRECTIONS-NEEDED (narrative only)** — the F5 required phrasings, folded into the decision above and this entry. Findings register F1–F5; two LOW follow-ups only if the C1 tooling is reused (widen the leakage-gate seed-frame loop; `run_c1.py` corpus-sha check). No verifier found a path that flips the verdict. Canonical record: `experiments/carrier-table-training/AUDIT_C1_2026-06-11.md`.
- **C2 mechanism pre-reg DRAFTED during the GPU wait** (discharges Round 31's "C2 drafts during C1's GPU waits"): `experiments/mechanism-style-content/criteria.DRAFT.md` — the style-vs-content probe (does the surviving wall track corpus **style** or payload **content**?), frozen-detector scoring of a same-generator 2×2 counterfactual corpus (P-orig / P-restyled / N-orig / N-styled), **11 TBDs flagged**; it awaits its **own present-first ratification** and authorizes no spend. C1's reduced-but-persisting frozen signature is cited there as consistent with a **mixed** style/content mechanism.
- **Unchanged / parked:** research_toolkit **PR #38** upstream merge (the repo-local clone already runs the fix — corpus generation was unaffected); the **v0.1.0 close stays HELD on accounts** (Fork A(b), Round 31); **DF-11/#93** carrier re-lock.

**Open forks** (registered, NOT decided — each awaits its own present-first ratification):
- **ADR-055 carrier-line amendment (the spine fork).** The residual table wall (+0.205 control at `lora`) is now shown **data-resistant at the ceiling**: format-matched, leakage-gated, in-axis training data reduced the frozen rung but did not close — or CI-supportedly reduce — the wall at the `lora` decision rung. Whether the ratified carrier line (**"capacity-attenuated, residual, table-concentrated (provisional, n=3)"**) gains **"…and data-resistant at the ceiling — not closable by targeted same-format synthetic data (n=1 fold, one recipe)"** is a **user ratification fork**, per the amendment-after-verdict precedent (ADR-055's dated 2026-06-01 carrier-LODO + 2026-06-06 cross-family sections). Until ratified, downstream spine prose is **unchanged**; the downstream surfaces the amendment would touch are registered below so ratification can scope the edit.
- **C2 ratification** — adjudicate TBD-1..11 + spend scope; a separate session.

**Implications**:
- **Phrasing discipline (audit F5) binds all downstream prose:** REDUCED/CLOSED/NOT-CLOSED labels exist **only at the `lora` ceiling** — say "frozen-rung reduction … did not survive the decision rung", never "frozen wall REDUCED" as a standalone verdict (the `5c90d1b` commit subject + a prior handoff title used the label informally; the write-gate correctly stayed closed); never "the treated wall grew"; B+ analogy scoped to "does-not-bridge at the `lora` ceiling"; tfidf move is val-side, the carrier-invariance reading stands.
- **Lane 2's hypothesis surface needs the result regardless of the spine fork:** `experiments/lane-2/hypothesis.md` + `protocol.md` frame the residual table wall as the gap Lane-2 training closes — the NOT-CLOSED datum is the direct (negative) answer for the synthetic format-matched recipe. Re-frame, don't delete: the H-optimistic null was pre-committed as publishable and feeds C2.
- **The mechanism question is sharpened, not answered:** part of the gap moves with format-matched data (frozen), none of it survives-the-move at the ceiling — exactly the signature C2's 2×2 is designed to decompose (style-borne vs content-borne).
- **Cost ledger:** +$1.74 base-budget (corpus $0.27 + `lora` $1.47); ADR-002's cap untouched; ADR-014 stays Reserved.
- **No §16 gate changes:** the carrier-study re-test gate (n=3→n=5, qa/abstract-conditional) is unchanged and would carry the data-resistance qualifier only if the amendment fork is taken.

**Next** (user-led):
- Decide the **ADR-055 carrier-line amendment fork** (present-first; if taken, draft the dated amendment section + the downstream edits registered below).
- **Ratify C2** (TBD adjudication + spend scope) — the queued next experiment per `roadmap-refresh-2026-06-09.md`, or re-prioritize at the next refresh.
- Commit + push the C1 datum/audit session work (commit trailer per repo convention).
- Parked, unchanged: the held v0.1.0 close (accounts); research_toolkit PR #38 upstream merge; DF-11/#93.

**Downstream surfaces if the ADR-055 amendment fork is taken** (registered now; each cites the spine/carrier line today):
- `decisions/ADR-055-post-m1-re-ladder-multi-axis-spine.md` — the amendment lands here as a new dated section (Decision 1's carrier line + the Carrier-LODO resolution's "honest spine claim" wording, :153–158).
- `decisions/README.md` — the ADR-055 index row carries the spine summary (:141).
- `docs/planning/PORTFOLIO_PLAN.md` — §9 M1→M2 entry-gate resolution prose (:1358), §16 Round-30 gates (carrier-LODO RESOLVED text :1646 + carrier-study re-test gate :1647), §5 Lane-2 framing, and the Round-30 update's bracketed refinement annotations (:696, :703).
- `README.md` — milestone table M2+ row (:138): "carrier SMALL-THROUGHOUT … next: Lane-2 carrier training" — the "next" clause is additionally stale now that the Lane-2 carrier-training arc has run.
- `M0_READINESS.md` — the corrected full-spine tag/announcement text (:160) quotes the carrier line; the held v0.1.0 close must ship whichever wording is ratified.
- `docs/glossary.md` — the capacity-attenuated / residual-wall entries (:182, :188–212, :462–464, :698).
- `docs/planning/roadmap-refresh-2026-06-09.md` — the STANDING SURFACE "Where the science stands" (:9) + spine block (:42) + its Fork-C table (C1 was "next"; now resolved).
- `experiments/lane-2/hypothesis.md` (:19, :50) + `experiments/lane-2/protocol.md` (:16) — the residual wall as Lane-2's live gap (needs the C1 result even without the amendment).
- `docs/planning/portfolio-chapter-outlines.md` — Ch 9 Lane-2 outline + the row-E decision criterion citing the residual table-carrier wall (:142, :167, :174).
- `docs/planning/forward-paths-explained.md` — the plain-language "stubborn gap on table-formatted inputs" passage (:25).
- `docs/planning/SESSION-HANDOFF.md` / `NEXT_SESSION.md` — prior handoff used "frozen wall REDUCED" informally (F5.1 phrasing correction; the 2026-06-11 PM handoff block already carries the corrected phrasing).
