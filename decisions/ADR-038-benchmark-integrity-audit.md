---
adr_id: "038"
slug: benchmark-integrity-audit
title: "M0 benchmark integrity audit — confirm no training-set overlap with held-out evals"
date: 2026-05-19
status: Accepted
linked_round: "R7 Tier A"
plan_section: "§5 + §9"
---

# ADR-038: Benchmark integrity audit

## Status

Accepted (Round 7 Tier A Q1'''''' — zero-cost methodology guardrail).

## Context

Per the compass artifact survey + Goodhart-law analysis in submission's
ADR-064 §C1, evaluating a detector on a benchmark dataset that overlaps
with the detector's training data inflates measured AUC. The submission
predecessor's V4 finding documented ~8.4pp aggregate AUC inflation random
vs source-disjoint LODO splits.

Portfolio's eval slates include benchmark suites with known prior training
distributions:
- **PINT** (Lakera): may have been trained on by ProtectAI v1/v2.
- **PromptShield**: training data not fully disclosed.
- **NotInject**: an over-defense probe with specific provenance.
- **HackAPrompt**: large public corpus; could appear in pretraining mixes.

Portfolio MUST confirm it doesn't repeat the integrity-failure mode.

## Decision

At M0 (sub-deliverable per plan §9), audit each portfolio training-corpus
input + each eval slate for overlap. Specifically:

1. Lane 2's MR-3 synthetic corpus (~10k indirect-injection positives +
   ~10k benign carriers) is **synthetic** + freshly generated by Claude
   Sonnet 4.6 per ADR-041 (ETHICS content lock). No overlap with PINT /
   PromptShield / NotInject / HackAPrompt by construction.
2. The submission's training pool (used to train the frozen-probe
   baseline) excludes PINT + PromptShield + NotInject + HackAPrompt per
   submission ADR-016 LODO + cross-source disjoint splits.
3. Reference scorers (ProtectAI v1/v2, Meta PG2 86M) carry their own
   training-data provenance; portfolio does NOT use them in training, so
   their training-overlap with portfolio's evals is a downstream concern,
   not portfolio's.
4. Test-contract `leakage_scan_present` (Day 3b) enforces a leakage scan
   reference in lane protocols.

## Consequences

- Zero additional cost ($0 — audit is a paper exercise + scan script).
- Documents the Goodhart-law discipline portfolio inherits from
  submission + extends with Round 7 Tier A formality.
- ETHICS.md §3 (Responsible use) cites this ADR to discourage
  detector-leaderboards that compare detectors at inconsistent operating
  points / training mixes.
- If Tier C PromptShield Llama-3.1-8B unlocks: confirm PromptShield's
  training data publicly + adjust report if overlap exists.

## Sprint 2 dossier evidence (added M0 close, Round 24)

The Sprint 2 literature dossier surfaces a 9-paper **`composition_audit`**
claim family in `docs/research/training-and-evaluation/` that
operationalizes contamination detection methodology. These carriers
inform both the audit step here and the future post-Tier-C re-audit:

- **`xu2024contamination`** + **`deng2024contamination`** —
  contamination survey + investigation across modern benchmarks
  (formalize the failure mode this ADR guards against).
- **`oren2023provetestcontam`** (NeurIPS 2023) — proves test-set
  contamination in black-box LLMs (methodology: exchangeability
  hypothesis test). Applicable to ProtectAI v1/v2 + Meta PG2 detector
  audits when their training data is opaque.
- **`shi2023minkprob`** (Min-K%-Prob) — token-likelihood-tail detection
  of pretraining data. Drop-in audit primitive for portfolio's training
  pool if any suspected leakage surfaces.
- **`zawalski2025codec`** (CoDeC) — in-context-learning-based
  contamination detection. Complement to MinK% for opaque detectors.
- **`yang2024rephrased`** + **`white2024livebench`** — rephrased-content
  contamination + contamination-limited benchmark design (informs
  Lane 2's synthetic-corpus + LODO discipline).
- **`sainz2023nlpeval`** + **`m2026sixfrontier`** — broader NLP
  contamination critique + 2026 frontier audit applied to LLM
  benchmarks. Validates the field-wide importance of this ADR.

If Tier C PromptShield Llama-3.1-8B unlocks at M2+, the portfolio MAY
run `shi2023minkprob` + `zawalski2025codec` as a confirmatory audit step
before reporting comparative results against PromptShield's evals.

Claim family anchored: `composition_audit` (9 entries). See
`docs/research/training-and-evaluation/bib_ledger.yml`.

## Cross-references

- Submission ADR-016 (LODO) + ADR-064 (Goodhart-discipline)
- V4 contamination signature finding (Round 7 Tier B)
- ADR-041 (ETHICS content lock; cites the synthetic-corpus provenance)
- `tests/contracts/test_leakage_scan_present.py` (Day 3b enforcement)
- ADR-048 (Round 24, cross-classification policy)
- `docs/research/training-and-evaluation/bib_ledger.yml` (claim_family:
  `composition_audit`)
