# C1 multi-verifier adversarial audit — 2026-06-11 — verdict NOT-CLOSED is ROBUST

**Scope.** The C1 carrier/table-training decision datum produced today: the treated-arm `lora` rung
(RunPod H100 80GB SECURE @ $3.29/h, pod 09:04:33→09:31:21 ≈ 26.8 min ≈ **$1.47** realized; pulled →
`C1_results_runpod_lora/`, merged sha-verified into `C1_results_treated/`) and the pre-locked
`c1_verdict.py` readout on the merged tree:

| rung | G_control | G_treated | ΔG (control−treated) | CI-low (1-sided 95%, 10k) | reading |
|---|---|---|---|---|---|
| tfidf | −0.1485 | −0.0129 | −0.1356 | −0.1365 | val-side move; see Required phrasing 3 |
| frozen | +0.3341 | +0.2511 | +0.0830 | +0.0788 | CI-supported frozen-rung reduction |
| **lora** | **+0.2054** | **+0.2333** | **−0.0279** | **−0.0319** | **NOT-CLOSED** (CI-low ≤ 0) |

**VERDICT = NOT-CLOSED** (rule fixed at ratification 2026-06-10, pre-datum): targeted, leakage-gated,
format-matched table data does not bridge the residual table wall at the `lora` ceiling; the
frozen-rung reduction does not survive capacity.

**Method.** Five independent adversarial verifiers (B4 pattern), each instructed to refute, run
concurrently against the on-disk artifacts (read-only; scratch recomputes in /tmp):

| role | charge | verdict line |
|---|---|---|
| 1 | independent numerical reproduction (no repo verdict code) | **REPRODUCES** — all 9 point values Δ=0.0 at 6 decimals; 6 pulled lora files byte-identical to merged; own paired bootstrap: lora ΔG ≈ 11.6 sd below 0, all draws negative, robust to cluster choice; CI-low deviation ≤ 0.0001 |
| 2 | implementation bug hunt (folds, augmentation, pairing, sha chain) | **SOUND** — treated test parquets row-identical *in order* to control for all 9 (seed,rung); W4 per-(iter,seed) fresh draws + exact cluster correspondence verified; corpus_sha256 `773108e1…` matches all 9 metrics incl. the 3 pod-produced; no silent exception path |
| 3 | leakage re-derivation (corpus vs val/test, all seeds) | **LEAK-FREE** — 0 exact / 0 normalized / 0 near-dup (5-gram Jaccard>0.8, full cross-product) for all 2400 corpus rows vs val+test across seeds 0/1/2; max similarity 0.205 vs test; context_sha256 0-overlap in all 9 cells |
| 4 | pre-registration compliance (criteria-as-ratified vs executed) | **COMPLIANT** — zero MATERIAL deviations across 10 commitments; append-only criteria, Revision 1 pre-datum provider-only, gate-before-training (mtimes+git), control untouched since 2026-06-01 with anchors matching ratified text, spend in envelope |
| 5 | claims-vs-artifacts + interpretation | **CORRECTIONS-NEEDED** (narrative only) — all headline numbers and the NOT-CLOSED label reproduce exactly; required phrasings below |

**Bottom line: ROBUST.** The numbers reproduce exactly, the implementation is sound, the corpus is
leak-free at every seed, the procedure matches the ratified pre-registration, and no verifier found a
path that flips the verdict. Every correction below is to the *narrative*, not the datum.

---

## Findings register

### F1 — Leakage-gate seed coverage (Roles 2+3, convergent; LOW, closed empirically)
The committed `leakage_gate.py` builds its test side from the **seed-0** frame only; seeds 1/2 sample
different table contexts, so ~85% of their test texts were never scanned by the committed gate. Both
verifiers independently re-ran exact + MinHash(0.8) scans against the seed-1/2 test sets and the full
seed-invariant 1000-context BIPIA table pool: **0 hits everywhere** — no actual leakage, and missed
leakage could only bias *toward* CLOSED (the verdict is NOT-CLOSED). Disposition: disclosure note
here; a follow-up to widen the gate's frame loop if C1 tooling is reused.

### F2 — Latent footgun in `run_c1.py` gate check (Role 2; LOW, not exercised)
`run_c1.py` checks `leakage_gate.json` *exists* but not that its `corpus_gated_sha256` matches the
`--corpus` argument. Not exercised here (the sha chain holds end-to-end). Follow-up if reused.

### F3 — Compliance disclosures (Role 4; MINOR, both pre-datum-locked)
(a) criteria.md said "per-arm CIs"; the implemented estimator is the **paired ΔG bootstrap** —
disclosed in the pre-datum commit (`6119111` "paired ΔG bootstrap") and the docstring; the rule needs
CI-low(ΔG), which per-arm CIs don't define; not verdict-changing under any variant. (b) The failed
Anthropic generation attempt's manifest was overwritten by the OpenAI run's manifest at the same path;
the "$0, no datum" claim is attested by commit history + timeline, not a surviving artifact.

### F4 — Stale merged-tree manifest (Roles 2+5; bookkeeping)
`C1_results_treated/MANIFEST.json` said `rungs: ["tfidf","frozen"]` after the lora merge (the verdict
globs the disk and never reads it). **Re-stamped 2026-06-11** with the merged rungs + lora provenance
(this audit). The pod's own lora-only manifest remains under `C1_results_runpod_lora/`.

### F5 — Required phrasings for Round 32 and all downstream prose (Role 5)
1. **REDUCED label scoping**: REDUCED/CLOSED/NOT-CLOSED are defined only at the `lora` ceiling. Say
   "frozen-rung reduction (ΔG +0.083, CI-supported) — did not survive the decision rung: lora verdict
   NOT-CLOSED", never "frozen wall REDUCED" as a standalone verdict (the `5c90d1b` commit subject and
   the prior handoff title used the label informally; the write-gate correctly stayed closed).
2. **Seed-conditionality of the lora ΔG CI**: the W4 bootstrap resamples payloads *conditional on the
   3 seeds* (val_roc fixed; no retrain noise). Control lora seed-0 is an outlier (test_roc 0.8366 vs
   0.7728/0.7690); dropping it moves ΔG −0.0279 → −0.0062, seed-level SE ≈ 0.0225 ≈ |ΔG|. Therefore:
   **never claim "the treated wall grew"** (the W13 lesson) — say "no evidence of reduction at lora
   (ΔG −0.028, payload-CI entirely <0 conditional on seeds); the negative direction is not
   seed-robust (−0.006 without control seed-0)". NOT-CLOSED is robust to this either way.
3. **tfidf re-label**: "the control's negative-gap artifact normalizes" is unsupported — no such
   artifact is defined anywhere; the ratified carrier-LODO reading of the negative tfidf gap is
   substantive ("no wall — lexical attack-string features are carrier-invariant") and **stands**. The
   move −0.148→−0.013 is **val-side** (val_roc 0.78→0.91; held-table test flat ~0.92–0.93): the corpus
   strengthens the lexical payload signal in-distribution. (Also: G_control(tfidf) is **−0.148**, not
   the handoff's "−0.149".)
4. **B+ pattern scope**: the analogy is pre-registered (criteria.md NOT-CLOSED branch) and fair at the
   pattern level, but say "does-not-bridge **at the lora ceiling**", not "same mechanism": C1's
   intervention is format-matched in-axis data that *did* reduce the frozen rung (ceiling-specific
   failure, which B+ never showed), and the axes differ (B+ = capacity-resistant cross-family; C1 =
   capacity-attenuated carrier).
5. **NotInject readout (pre-registered secondary, previously unreported)**: no over-defense price was
   paid — and no wall bought down. Treated lora notinject_fpr 0.655/0.681/0.779 (mean 0.705) vs
   control 0.637/0.779/0.770 (mean 0.729), n=113; clean_context_fpr seed-unstable in both arms
   (treated 0.123/0.026/0.422 vs control 0.213/0.000/0.466). Carry the n=113 + instability caveats.
6. **W10 dual reading** (verified present in `c1_verdict.json`): sign-only "wall persists (G=+0.233)"
   AND ½·G(frozen) "holds at the knob (0.233 ≥ 0.126)"; knob-choice-robust (also vs ½·G_frozen^control
   = 0.167). The "small under the knob" softening is correctly NOT invoked.

---

**Spend reconciliation.** Corpus $0.27 (OpenAI gpt-4.1-mini, Revision 1) + lora ≈ $1.47 (H100 SECURE
$3.29/h × 26.8 min; cap $8 untouched) ≈ **$1.74 total** for the C1 decision datum.

**Raw verifier reports.** Produced in-session by five concurrent subagents (transcripts in the session
task store); their verdict lines and findings are fully transcribed above. Verdict artifact:
`c1_verdict.json` (first write through the W3 gate; never overwritten).
